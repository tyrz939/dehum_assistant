# =================================================================
# Dehumidifier AI Service Configuration (.env example)
# =================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# -----------------------------------------------------------------
# REQUIRED
# -----------------------------------------------------------------
# OpenAI API key – needed for all LLM calls
OPENAI_API_KEY=

# -----------------------------------------------------------------
# OPTIONAL – Service-level auth (FastAPI -> WordPress / front-end)
# -----------------------------------------------------------------
# If set, every request must include header:  Authorization: Bearer <API_KEY>
# API_KEY=

# -----------------------------------------------------------------
# OPTIONAL – WordPress session storage
# -----------------------------------------------------------------
# Only needed if you enable the WordPress SessionStore in ai_agent.py
# WP_API_KEY=
# WORDPRESS_URL=http://localhost/your-wordpress-site

# -----------------------------------------------------------------
# OPTIONAL – Model + generation settings
# -----------------------------------------------------------------
# DEFAULT_MODEL=gpt-4o
# TEMPERATURE=0.3           # 0.0–2.0  (higher = more creative)
# THINKING_MODEL=gpt-4o-mini  # separate lightweight model, if desired

# -----------------------------------------------------------------
# OPTIONAL – Service network config
# -----------------------------------------------------------------
# SERVICE_HOST=0.0.0.0
# PORT=8000

# -----------------------------------------------------------------
# OPTIONAL – Logging & rate limiting
# -----------------------------------------------------------------
# LOG_LEVEL=INFO
# MAX_REQUESTS_PER_MINUTE=60
# MAX_TOKENS_PER_REQUEST=1000

# -----------------------------------------------------------------
# OPTIONAL – CORS (comma-separated). Use "*" for dev only.
# -----------------------------------------------------------------
# CORS_ORIGINS=*